% KDP 5x8"
\usepackage[12pt]{extsizes}
\usepackage[paperwidth=12.7cm,paperheight=20.32cm,lmargin=2.3cm,rmargin=1.8cm,bmargin=2.3cm]{geometry}
\widowpenalty=10000
\clubpenalty=10000

% Meilleure coupe des mots
\usepackage{microtype}

% No numeration on empty pages
\usepackage{emptypage}

% Langue française
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage[frenchb]{babel}

\setlength{\emergencystretch}{3em} % prevent overfull lines
% Les deux lignes suivantes sont nécessaire pour avoir \ldots{}
% correctement espacé en français
\usepackage[xspace]{ellipsis}
\renewcommand{\ellipsisgap}{0.1em} %  or another value

% Numérotation en pied de page
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyfoot[C]{\normalsize \thepage}
\renewcommand{\headrulewidth}{0pt}

% Heavier fonts in PDF rendering
\usepackage{color}
\usepackage{pdfrender}

% Nice separator
\newcommand{\ornament}{\begin{samepage}
\vskip 0.8\baselineskip
\centerline { $ * $ }
\nopagebreak
\centerline { $ * \quad * $ }
\vskip 0.8\baselineskip
\end{samepage}
}

% En-têtes (bold + centered)
\newcommand{\entete}[1]{\noindent \begin{center}\textbf{#1}\end{center} \par }
\newcommand{\doubletete}[2]{\noindent \begin{center}\textbf{#1 \linebreak #2}\end{center} \par }

% For the dedication above chapter I
\usepackage{quotchap}
\renewcommand*{\sectfont}{\rmfamily\huge\bfseries\selectfont}

% DEBUG
%\usepackage{showframe}
